interface:
  display_name: "Ultradetail Walk"
  short_description: "Exhaustive one-shot persona walk-through."
  default_prompt: "Use $ultradetail-walk to click through every relevant interaction once with normal and adversarial personas."
policy:
  allow_implicit_invocation: true
