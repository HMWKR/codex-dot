interface:
  display_name: "Harness Loop"
  short_description: "Coordinate agent work with quality gates."
  default_prompt: "Use $harness-loop to run a bounded Codex agent orchestration loop with verification gates."
policy:
  allow_implicit_invocation: true
